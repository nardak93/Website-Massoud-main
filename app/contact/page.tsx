// page.tsx in app/(default)/contact/

import ContactForm from '@/app/(default)/contact/ContactForm.client';

const ContactPage = () => {
    return (
        <div>
            {/* Other page content */}
            <ContactForm />
        </div>
    );
};

export default ContactPage;
