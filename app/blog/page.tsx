// app/(default)/blog/page.tsx
'use client';
import BlogPage from "../(default)/blog/Blog.client";

const Blog = () => {
  return (
    <div>
      {/* You can add additional page content here if needed */}
      <BlogPage />
    </div>
  );
};

export default Blog;
